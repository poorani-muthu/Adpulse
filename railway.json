{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "java -jar $(find /app/target -name '*.jar' | head -1)",
    "restartPolicyType": "ON_FAILURE"
  }
}
